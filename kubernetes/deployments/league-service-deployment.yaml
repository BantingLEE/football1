apiVersion: apps/v1
kind: Deployment
metadata:
  name: league-service
  namespace: football-manager
  labels:
    app: league-service
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: league-service
  template:
    metadata:
      labels:
        app: league-service
        version: v1
    spec:
      containers:
        - name: league-service
          image: football-manager/league-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3007
              name: http
          envFrom:
            - configMapRef:
                name: league-service-config
            - secretRef:
                name: mongodb-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
