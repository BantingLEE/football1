MONGO_INITDB_ROOT_USERNAME=admin
# SECURITY WARNING: Change this password immediately before deployment!
# Use a strong password with at least 20 characters, including uppercase, lowercase, numbers, and special characters
MONGO_INITDB_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE_MIN_20_CHARS
RABBITMQ_DEFAULT_USER=admin
# SECURITY WARNING: Change this password immediately before deployment!
# Use a strong password with at least 20 characters, including uppercase, lowercase, numbers, and special characters
RABBITMQ_DEFAULT_PASS=CHANGE_ME_STRONG_PASSWORD_HERE_MIN_20_CHARS
# SECURITY WARNING: Generate a secure random JWT secret before deployment!
# Use: openssl rand -base64 64 or similar
JWT_SECRET=CHANGE_ME_GENERATE_SECURE_RANDOM_64_BYTE_SECRET_BEFORE_DEPLOYMENT
